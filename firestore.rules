
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default locked mode. All reads and writes are denied.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
