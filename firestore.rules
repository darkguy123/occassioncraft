
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
  
    // Helper function to check for admin role
    function isAdmin() {
      return request.auth != null && exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }
  
  	// Helper function to check if a user owns a document
    function isOwner(userId) {
    	return request.auth != null && request.auth.uid == userId;
    }

    match /users/{userId} {
      // Admins can list all users.
      allow list: if isAdmin();
      // Users can read and write their own profile data. Admins can read/write any user's data.
      allow read, write: if isOwner(userId) || isAdmin();
    }
    
    // Users can read/write their own notifications.
    match /users/{userId}/notifications/{notificationId} {
      allow read, write: if isOwner(userId) || isAdmin();
    }

    // Users can read/write their own tickets.
    match /users/{userId}/tickets/{ticketId} {
      allow read, write: if isOwner(userId) || isAdmin();
    }
    
    match /events/{eventId} {
       // Admins can list all events.
       allow list: if isAdmin();
       // Events are publicly readable.
       allow read: if true;
       // Events can be written by their vendor owner or an admin.
       allow write: if isAdmin() || (request.resource.data.vendorId == request.auth.uid);
    }
    
    match /categories/{categoryId} {
    	 allow read: if true;
       allow write: if isAdmin();
    }

    match /vendors/{vendorId} {
      // Admins can list all vendors.
      allow list: if isAdmin();
      // Vendors can read and write their own profiles. Admins have full access.
      allow read, write: if isOwner(vendorId) || isAdmin();
    }
    
    // Wallets are private to the user or an admin.
    match /wallets/{userId} {
      allow read, write: if isOwner(userId) || isAdmin();
    }
    
    match /wallets/{userId}/transactions/{transactionId} {
       allow read, write: if isOwner(userId) || isAdmin();
    }
    
    // Admin role definitions are write-only for other admins, or for a user creating their own.
    match /roles_admin/{userId} {
    	allow read: if isAdmin();
      allow write: if isAdmin() || isOwner(userId);
    }
  }
}
