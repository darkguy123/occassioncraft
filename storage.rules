
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Temporary rule for debugging: Allow all reads and writes.
      // This is insecure and should be changed back.
      allow read, write;
    }
  }
}
