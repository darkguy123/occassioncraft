rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // WARNING: This is a temporary rule for diagnostics.
    // It allows anyone to read and write to your storage.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
